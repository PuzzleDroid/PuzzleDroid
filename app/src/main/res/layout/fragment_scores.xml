<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    tools:context=".ui.scores.ScoresFragment">

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >

        <TextView
            android:id="@+id/score_level_label"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:paddingVertical="30dip"
            android:paddingHorizontal="20dip"
            android:textColor="#CC0033"
            android:textSize="24sp"
            android:text="Score"
            android:layout_alignParentRight="true"/>

        <TextView
            android:id="@+id/score_time_label"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:layout_marginRight="12dp"
            android:layout_toLeftOf="@+id/score_level_label"
            android:paddingHorizontal="20dp"
            android:paddingVertical="30dp"
            android:text="Nickname"
            android:textColor="#CC0033"
            android:textSize="24sp" />
    </RelativeLayout>

    <ListView
        android:id="@+id/score_list_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

    </ListView>

</LinearLayout>