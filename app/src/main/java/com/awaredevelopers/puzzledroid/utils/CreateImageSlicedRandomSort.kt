package com.awaredevelopers.puzzledroid.utils

import android.graphics.Bitmap

class CreateImageSlicedRandomSort {
    open fun setRandomSort(bmp: Map<String, Bitmap>): Map<String, Bitmap> {
        val mutableMap: MutableMap<String, Bitmap> = mutableMapOf<String, Bitmap>()

        return mutableMap
    }
}